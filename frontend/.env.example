# URL du backend FastAPI (ParseAndCutV2).
# Laisser VIDE en prod : nginx sert le frontend et reverse-proxy /api/* vers le
# backend local sur le Pi (même domaine parseandcut.alithiel31.dev, pas de CORS
# nécessaire). Ne renseigner que pour du dev local pointant sur uvicorn direct.
VITE_API_URL=http://localhost:5000
